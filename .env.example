# Copy to .env for local development. In the bootc image these are set in the
# systemd unit instead.
IM_TRAIN_DB_URL=postgresql://imtrain:imtrain@im-train-db.rh-lab.labs:5432/imtrain
IM_TRAIN_HOST=0.0.0.0
IM_TRAIN_PORT=8080
